server:
  port: 8002
spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: replica1
  client:
    fetch-registry: true # 是否要从注册中心获取服务
    register-with-eureka: true # 是否要注册到注册中心
    service-url:
      defaultZone: http://replica2:8003/eureka/


